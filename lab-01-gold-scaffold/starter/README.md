# LAB-01 Starter

This starter pack loads environment values, reads the sample response, prints basic routing/logging output, and leaves stop routing intentionally thin.

The solution pack shows the fuller request skeleton and expanded stop-routing branches.

## Task

Expand the routing logic so the scaffold can explain what to do for each meaningful `stop_reason`.
