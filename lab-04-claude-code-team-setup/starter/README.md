# LAB-04 Starter

The starter pack is intentionally incomplete. It is missing safe deny rules and does not fully describe the reusable skill workflow.

Expected validator result:

```text
Pack validation failed:
- Add deny rules for .env and secrets access
- Skill is missing a workflow section
```

The starter still uses the current project skill location:

```text
.claude/skills/architecture-review/SKILL.md
```
