# LAB-04 Solution

The solution pack includes:

- repo-level `CLAUDE.md`
- project `.claude/settings.json` with safe deny rules
- a reusable architecture-review skill at `.claude/skills/architecture-review/SKILL.md`
- an example `.mcp.json` with top-level `mcpServers`
- a validator to keep the pack honest

Run:

```bash
python3 validate_pack.py
```

Expected result:

```text
Pack validation passed.
```
